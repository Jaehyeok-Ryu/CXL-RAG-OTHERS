# Private Server IP configurations
# Copy this file to .env and configure the IP addresses for your environment.
# .env is ignored by git to prevent exposing private server details.

CPU_SERVER_IP=127.0.0.1
GPU_SERVER_IP=127.0.0.1
VECTORDB_IP_1=127.0.0.1
VECTORDB_IP_2=127.0.0.1

# Specific IP configurations (Optional, falls back to CPU_SERVER_IP / GPU_SERVER_IP)
# EMBEDDING_CPU_SERVER_IP=127.0.0.1
# EMBEDDING_GPU_SERVER_IP=127.0.0.1
# REQUEST_GEN_GPU_SERVER_IP=127.0.0.1
